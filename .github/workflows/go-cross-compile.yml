name: Go Cross-Compile

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    name: Build Go Executables
    runs-on: ubuntu-latest

    strategy:
      matrix:
        goos: [linux, windows, darwin]
        goarch: [amd64, arm64]
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.21'
          cache: true

      - name: Compile binaries
        run: |
          mkdir -p dist
          # Build panago
          GOOS=${{ matrix.goos }} GOARCH=${{ matrix.goarch }} \
            go build -o dist/panago-${{ matrix.goos }}-${{ matrix.goarch }} ./cmd/panago
          
          # Build shell
          GOOS=${{ matrix.goos }} GOARCH=${{ matrix.goarch }} \
            go build -o dist/shell-${{ matrix.goos }}-${{ matrix.goarch }} ./cmd/shell
          
          # Build diskutil
          GOOS=${{ matrix.goos }} GOARCH=${{ matrix.goarch }} \
            go build -o dist/diskutil-${{ matrix.goos }}-${{ matrix.goarch }} ./cmd/diskutil
          
          # Build discover
          GOOS=${{ matrix.goos }} GOARCH=${{ matrix.goarch }} \
            go build -o dist/discover-${{ matrix.goos }}-${{ matrix.goarch }} ./cmd/discover

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: binaries-${{ matrix.goos }}-${{ matrix.goarch }}
          path: dist/
